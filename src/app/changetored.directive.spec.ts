import { ChangetoredDirective } from './changetored.directive';

describe('ChangetoredDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangetoredDirective();
    expect(directive).toBeTruthy();
  });
});
